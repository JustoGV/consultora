'use client';

import AfiliadoForm from '../AfiliadoForm';

/** Alta de afiliado — formulario todo-en-uno (titular + afiliación + adherentes). */
export default function NuevoAfiliadoPage() {
  return <AfiliadoForm persona={null} />;
}
